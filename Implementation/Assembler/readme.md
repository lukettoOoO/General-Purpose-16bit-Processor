### Assembler usage:
`python3 assembler.py -i <input_file.asm> -o <output_base_name>`
### Example:
`python3 assembler.py -i test.asm -o test_output`

Generates both `.txt` (human-readable binary) and `.bin` (ROM-loadable binary) files.
